import type { RegionArt } from './types';

/** New England & the Atlantic region art library. Stub — filled in Phase B. */
export const REGION_ART_ATLANTIC: RegionArt = { regionId: 'atlantic', entries: [] };
