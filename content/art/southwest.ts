import type { RegionArt } from './types';

/** Desert Southwest region art library. Stub — filled in Phase B. */
export const REGION_ART_SOUTHWEST: RegionArt = { regionId: 'southwest', entries: [] };
